[project]
name = "maiascan"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pybind11-stubgen>=2.5.5",
    "pytest>=9.0.2",
]
